#include <iostream>

int main() {
    // The compiler treats comment as white space.
    /* 
    It's a multiline comment. 
    isn't it ? 
    */
   // Single line comment.
    std::cout << "Mohd Taufeeq Khan";
    return 0;
}